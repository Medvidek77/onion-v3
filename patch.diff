--- shader.comp
+++ shader.comp
@@ -10,13 +10,13 @@
  * - Uses array indexing instead of struct to avoid SPIR-V bloat
  * - Uses stable Workgroup layout for optimal occupancy
  */
-layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;
-const uint WG_SIZE = 128u;
+layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;
+const uint WG_SIZE = 64u;

-shared int shared_P[10][WG_SIZE];
-shared int shared_S[10][WG_SIZE];
+shared int shared_P[WG_SIZE][10];
+shared int shared_S[WG_SIZE][10];
 shared int shared_total_inv[10];
 shared int s_base_YplusX[10];
 shared int s_base_YminusX[10];
@@ -140,8 +140,8 @@
                            offsets[id].YplusX, offsets[id].YminusX, offsets[id].Z, offsets[id].T2d);

         for (int i = 0; i < 10; i++) {
-            shared_P[i][lid] = R_Z[i];
-            shared_S[i][lid] = R_Z[i];
+            shared_P[lid][i] = R_Z[i];
+            shared_S[lid][i] = R_Z[i];
         }
     } else {
         int one[10];
@@ -149,8 +149,8 @@
             one[i] = (i == 0) ? 1 : 0;
         }
         for (int i = 0; i < 10; i++) {
-            shared_P[i][lid] = one[i];
-            shared_S[i][lid] = one[i];
+            shared_P[lid][i] = one[i];
+            shared_S[lid][i] = one[i];
         }
     }

@@ -165,16 +165,16 @@

         if (do_P) {
             for (int i = 0; i < 10; i++) {
-                P_curr[i] = shared_P[i][lid];
-                P_prev[i] = shared_P[i][lid - stride];
+                P_curr[i] = shared_P[lid][i];
+                P_prev[i] = shared_P[lid - stride][i];
             }
             fe_m(temp_P, P_curr, P_prev);
         }

         if (do_S) {
             for (int i = 0; i < 10; i++) {
-                S_curr[i] = shared_S[i][lid];
-                S_next[i] = shared_S[i][lid + stride];
+                S_curr[i] = shared_S[lid][i];
+                S_next[i] = shared_S[lid + stride][i];
             }
             fe_m(temp_S, S_curr, S_next);
         }
@@ -182,15 +182,15 @@
         barrier();

         if (do_P) {
-            for (int i = 0; i < 10; i++) shared_P[i][lid] = temp_P[i];
+            for (int i = 0; i < 10; i++) shared_P[lid][i] = temp_P[i];
         }
         if (do_S) {
-            for (int i = 0; i < 10; i++) shared_S[i][lid] = temp_S[i];
+            for (int i = 0; i < 10; i++) shared_S[lid][i] = temp_S[i];
         }
     }

     if (lid == WG_SIZE - 1u) {
-        for (int i = 0; i < 10; i++) last_P[i] = shared_P[i][WG_SIZE - 1u];
+        for (int i = 0; i < 10; i++) last_P[i] = shared_P[WG_SIZE - 1u][i];
         fe_invert(shared_total_inv, last_P);
     }

@@ -201,11 +201,11 @@
         if (lid == 0) {
             for (int i = 0; i < 10; i++) P_excl[i] = (i == 0) ? 1 : 0;
         } else {
-            for (int i = 0; i < 10; i++) P_excl[i] = shared_P[i][lid - 1];
+            for (int i = 0; i < 10; i++) P_excl[i] = shared_P[lid - 1][i];
         }

         int S_excl[10];
         if (lid == WG_SIZE - 1u) {
             for (int i = 0; i < 10; i++) S_excl[i] = (i == 0) ? 1 : 0;
         } else {
-            for (int i = 0; i < 10; i++) S_excl[i] = shared_S[i][lid + 1];
+            for (int i = 0; i < 10; i++) S_excl[i] = shared_S[lid + 1][i];
         }
--- main.c
+++ main.c
@@ -21,7 +21,7 @@

 #define BATCH_SIZE (1 << 22) /* 2,097,152 */
 #define NUM_THREADS 12       /* Max CPU threads for Ryzen 5 5600 */
-#define WORKGROUP_SIZE 128
+#define WORKGROUP_SIZE 64


 /* Internal CPU structs mapping directly to GPU buffers */
